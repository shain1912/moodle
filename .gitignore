# secrets / env
.env
.env.local
_secrets.txt

# 학생 개인정보 / 원본 자료 (절대 커밋 금지)
*.xlsx
image.png

# 강의 영상 원본/변환본 (용량 큼 — R2에만 올림)
uploads/
*.zip
*.mkv
*.mp4
*.mov

# 로컬 운영 스크립트 (로컬 경로 참조)
_*.mjs
_*.log

# deps / build
node_modules/
*.log
.DS_Store
Thumbs.db

# tooling
.agents/
skills-lock.json

# 로컬 MCP 설정
.mcp.json
